Installed {{ .Chart.Name }}…

1. Get the currently registered SPIFFE entries from the server:

  kubectl exec -n {{ .Release.Namespace }} {{ include "spire-server.fullname" . }}-0 -c spire-server -- \
    spire-server entry show

{{- if not .Values.tornjak.config.backend.ingress }}
No Tornjak Backend Ingress provided. 
Use port forwarding to access the Tornjak Backend port:
      kubectl -n {{ include "spire-server.namespace" . }} port-forward {{ include "spire-server.fullname" . }}-0 10000:10000
{{- end }}

{{- if not .Values.tornjak.config.frontend.ingress }}
No Tornjak Frontend Ingress provided. 
Use port forwarding to access the Tornjak Frontend port:
      kubectl -n {{ include "spire-server.namespace" . }} port-forward {{ include "spire-server.fullname" . }}-0 3000:3000
{{- end }}

Tornjak API (Backend) access:   {{ include "tornjak.apiURL" . }}
Tornjak UI (Frontend) access:   {{ include "tornjak.FrontendURL" . }}
