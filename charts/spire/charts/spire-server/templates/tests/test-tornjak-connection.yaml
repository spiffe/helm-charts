{{- if eq (.Values.tornjak.enabled | toString) "true" }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spire-tornjak.fullname" . }}-test-connection"
  namespace: {{ include "spire-server.namespace" . }}
  labels:
<<<<<<< HEAD
    {{- include "spire-server.labels" . | nindent 4 }}
=======
>>>>>>> 27549a3 (Update the test to newer version)
  annotations:
    "helm.sh/hook": test
spec:
  securityContext:
    {{- toYaml .Values.podSecurityContext | nindent 4 }}
  containers:
    - name: wget-tornjak-backend
      image: cgr.dev/chainguard/bash:latest
      command: ['curl']
      args: ['-k', '-s', '-f', 'http://{{ include "spire-tornjak.backend" . }}.{{ include "spire-server.namespace" . }}.svc.{{ include "spire-server.cluster-domain" . }}:10000/']
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
  restartPolicy: Never
{{- end }}
