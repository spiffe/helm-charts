spiffe-oidc-discovery-provider:
  enabled: true
  insecureScheme:
    enabled: true

agent-upstream:
  enabled: true
  server:
    address: spire-server.nested-deps

csi-driver-upstream:
  enabled: true
